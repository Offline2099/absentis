export enum ArticleElement {
  subheader = 'subheader',
  paragraph = 'paragraph',
  quote = 'quote',
  epigraph = 'epigraph',
  image = 'image',
  video = 'video',
  separator = 'separator',
  attribution = 'attribution',
  referenceList = 'references'
}