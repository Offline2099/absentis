export enum ArticleElement {
  paragraph = 'paragraph',
  quote = 'quote',
  epigraph = 'epigraph',
  image = 'image',
  video = 'video',
  separator = 'separator',
  referenceList = 'references'
}